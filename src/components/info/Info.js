import React from "react";

const Info = props => (
    <div className="container p-0 m-0 w-100" style={{maxWidth:"100%"}}>
        <div className=" py-5 bg-dark p-0 m-0 w-100" >
            <h1 className="text-light text-center">Clicky Game!</h1>
            <h3 className ="text-light text-center">Click on an image to earn points, but don't click on any more than once!</h3>
        </div>
    </div>
);

export default Info